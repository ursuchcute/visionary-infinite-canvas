# Temporary first-certificate vhost. Install this file before running:
# certbot certonly --nginx -d canvas.visionary.beer
server {
    listen 80;
    listen [::]:80;
    server_name canvas.visionary.beer;

    location / {
        return 404;
    }
}
